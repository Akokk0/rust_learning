fn main() {
    // 加
    let sum = 5 + 10;

    // 减
    let difference = 95.5 - 4.3;

    // 乘
    let product = 4 * 30;

    // 除
    let quotient = 56.7 / 32.2;

    // 取余
    let reminder = 54 % 5;

    // Rust 支持对浮点类型取余（两边都是同类型浮点数才行）
    // 例如： 1.0 % 0.3 = 0.1
}